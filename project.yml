name: Recap
options:
  bundleIdPrefix: com.gregfoster
  deploymentTarget:
    macOS: "26.0"
  xcodeVersion: "26.0"
  createIntermediateGroups: true

packages:
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    from: 2.0.0
  RecapCore:
    path: Packages/RecapCore
  RecapAudio:
    path: Packages/RecapAudio
  RecapTranscription:
    path: Packages/RecapTranscription
  RecapEnhancement:
    path: Packages/RecapEnhancement
  RecapUI:
    path: Packages/RecapUI

targets:
  Recap:
    type: application
    platform: macOS
    sources:
      - Recap
    dependencies:
      - package: RecapCore
      - package: RecapAudio
      - package: RecapTranscription
      - package: RecapEnhancement
      - package: RecapUI
      - package: Sparkle
    info:
      path: Recap/Info.plist
      properties:
        CFBundleDisplayName: Recap
        CFBundleShortVersionString: "0.11.0"
        CFBundleVersion: "51"
        LSMinimumSystemVersion: "26.0"
        LSApplicationCategoryType: public.app-category.productivity
        NSMicrophoneUsageDescription: Recap records your microphone during meetings you start. Audio never leaves your Mac.
        NSAudioCaptureUsageDescription: Recap records system audio so other meeting participants are captured. Audio never leaves your Mac.
        NSCalendarsFullAccessUsageDescription: Recap reads your calendar to notice when a meeting is starting, so it can offer to record. Event details never leave your Mac.
        NSHumanReadableCopyright: © 2026 Greg Foster. MIT License.
        # Finder "Open With" for audio files → application(_:open:) → import.
        # Viewer/Alternate: Recap can open audio but never claims default-app.
        CFBundleDocumentTypes:
          - CFBundleTypeName: Audio
            CFBundleTypeRole: Viewer
            LSHandlerRank: Alternate
            LSItemContentTypes:
              - public.audio
        SUFeedURL: https://gregorymfoster.github.io/recap/appcast.xml
        SUPublicEDKey: WsqMrsc1Rv980zEPBuwQtawmJ0em/FshzcflTky6naY=
        SUEnableAutomaticChecks: true
    entitlements:
      path: Recap/Recap.entitlements
      properties:
        com.apple.security.device.audio-input: true
        com.apple.security.personal-information.calendars: true
    settings:
      base:
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        ENABLE_HARDENED_RUNTIME: true
        PRODUCT_BUNDLE_IDENTIFIER: com.gregfoster.recap
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: 2V7W69N399
        MARKETING_VERSION: "0.11.0"
        CURRENT_PROJECT_VERSION: "51"
        ENABLE_USER_SCRIPT_SANDBOXING: true

schemes:
  Recap:
    build:
      targets:
        Recap: all
    run:
      config: Debug
    archive:
      config: Release
